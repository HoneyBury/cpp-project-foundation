# Security Policy

Report vulnerabilities privately through GitHub Security Advisories. Do not open a
public issue containing exploit details, credentials, tokens, private keys, or production
host information.

The supported line is the latest tagged `0.x` release. GitHub Actions are pinned to full
commit SHAs, release jobs use least-privilege permissions, and dependency, sanitizer and
fuzz maintenance runs weekly.
