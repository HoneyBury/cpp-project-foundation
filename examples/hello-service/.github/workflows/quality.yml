name: Quality / C++ Foundation
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  cpp-quality:
    uses: HoneyBury/cpp-project-foundation/.github/workflows/reusable-quality.yml@v0.3.1
    with:
      conan-profile: conan/profiles/linux-clang-x64
      coverage-profile: conan/profiles/linux-gcc-x64
      conan-lockfile: conan/locks/linux-gcc-x64-release.lock
      cmake-target: hello_service
      test-target: hello_service_tests
